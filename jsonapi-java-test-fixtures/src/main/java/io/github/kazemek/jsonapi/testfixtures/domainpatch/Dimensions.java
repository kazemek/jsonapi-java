package io.github.kazemek.jsonapi.testfixtures.domainpatch;

/** Ordinary structured domain value type with a primitive nested member (ADR-014). */
public record Dimensions(double width, double height) {}
